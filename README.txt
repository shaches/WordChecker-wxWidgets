This gets user input from a grafical user interface and converts them into separated words with multiple additional functionalities:
1: Program counts every word occurence, sorts them and displays it in the GUI.
2: Program can filter some words that are unwanted in output.
3: Program input and output can be saved to a user defined place as a file.

